

# DataSource configuration
spring.datasource.url=jdbc:mysql://centerbeam.proxy.rlwy.net:41018/railway
spring.datasource.username=root
spring.datasource.password=VTEacIElFrTGveLAGnKcUhQAYAAfSAUR
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect
spring.jpa.hibernate.ddl-auto=update
eureka.client.service-url.defaultZone=https://protective-determination-production.up.railway.app/eureka/

# Set the public hostname that Eureka should use
eureka.instance.hostname=spirited-liberation-production.up.railway.app

# Specify the external non-secure port (if your service is available via HTTP)
# If it's available via HTTPS, you might need to set securePort and enable it.
eureka.instance.nonSecurePort=80
eureka.instance.securePortEnabled=false

# Do not prefer the internal IP address
eureka.instance.preferIpAddress=false


# Eureka Client Configuration

eureka.instance.prefer-ip-address=true
eureka.client.register-with-eureka=true
eureka.client.fetch-registry=true





# JPA configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# Logging configuration
logging.level.org.springframework.cloud.netflix.eureka=DEBUG
logging.level.org.springframework.web=DEBUG
logging.level.org.springframework.security=DEBUG

# JWT Configuration
jwt.secret=dgjorufhrsnjdu438fkdj38fdmcv7dm3ckvhrsnjjuwelueivhe848fhedldh5ndk
jwt.expiration=604800000

# Server Port
server.port=${PORT:8080}

# Email Configuration
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=kamarelngunda@gmail.com
spring.mail.password=hmvqzpwtstzizmde
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=3000
spring.mail.properties.mail.smtp.writetimeout=5000

# application.properties in the microservice
gateway.security.key=MyApiGateway

# application.properties in the microservice
gateway.security.key=3H#7D*faG&L^2z!9Xy@uKp6T

spring.datasource.hikari.maximum-pool-size=20
spring.datasource.hikari.minimum-idle=5
spring.datasource.hikari.idle-timeout=30000

